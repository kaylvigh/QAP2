version: '3'
services:
  rest-api:
    image: kaylvigh/qap2-image:latest
    ports:
      - 8080:8080
    environment:
      - spring.datasource.url=jdbc:mysql://host.docker.internal:3306/schwema